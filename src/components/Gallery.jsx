import React from 'react';
import Cards from './Cards';

const Gallery = () => {
    return (
        <div className="container border">
            <h1>Gallery App</h1>
            <Cards />
        </div>
    );
};

export default Gallery;
